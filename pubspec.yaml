name: f_shake_animation_widget
description: Flutter抖动动画组件
number: 1
version: 0.0.1
homepage: https://github.com/goodswifter/f_shake_animation_widget.git

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
